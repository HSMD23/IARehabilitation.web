@model IEnumerable<IARehabilitation.web.Data.Entities.TreatmentDictionary>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Treatment_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Treatment_length)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Treatment_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Treatment_length)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_TreatmentDictionary">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_TreatmentDictionary">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_TreatmentDictionary">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
